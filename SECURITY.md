# 安全政策

## 支持范围

项目当前处于 2.x 重构开发阶段，尚未发布新的正式版本。我们仍接受对当前默认分支、活动中的 2.x 开发分支以及最新正式版本的漏洞报告。历史分支、已停止维护的版本和第三方服务本身仅提供尽力而为的协助。

## 私密报告漏洞

请不要通过公开 Issue、讨论区或 Pull Request 披露未修复漏洞。

1. 优先使用代码托管平台的 **Private vulnerability reporting / Security Advisory** 功能提交报告。
2. 如果仓库未启用私密报告，请创建一个不包含漏洞细节的公开 Issue，仅说明需要安全联系渠道；维护者会提供私密沟通方式。
3. 不要在报告中附带真实密码、Token、Cookie、生产数据库或其他用户数据。请使用脱敏样例。

报告应尽量包含：

- 受影响版本、分支、提交或部署方式；
- 漏洞类型、影响范围和最坏情况；
- 可重复的最小步骤或概念验证；
- 已知前置条件及缓解办法；
- 是否已向第三方或其他人员披露。

我们目标在 3 个工作日内确认收到报告，在 7 个工作日内给出初步评估。修复时间取决于严重程度和发布风险；修复发布前请保持信息私密。

## 安全问题范围

通常属于安全问题：

- 身份认证、会话、密码重置或 CSRF 绕过；
- 跨用户读取或修改投资账户、持仓和自选数据；
- SQL 注入、命令执行、路径遍历、服务端请求伪造或跨站脚本；
- 密钥、个人信息或敏感日志泄露；
- 可被利用的数据源投毒、依赖供应链问题或权限提升；
- 能造成持久数据破坏或绕过关键业务约束的问题。

通常不属于安全漏洞：

- 免费行情或基金净值的延迟、缺失、第三方错误及服务不可用；
- 投资判断、收益预测或市场表现与预期不同；
- 未证明实际安全影响的版本扫描结果；
- 由自托管者公开数据库、弱化反向代理或泄露环境变量造成的配置问题；
- 已有速率限制下的低影响资源消耗，且无法导致跨用户影响或长期不可用。

如果无法判断，请按安全问题私密报告，我们会协助分类。

## 协调披露

确认漏洞后，维护者会评估严重程度、准备测试与修复、检查相邻风险，并在适当版本中发布。安全公告会说明受影响范围、升级或缓解方式，并在报告人同意时致谢。未经报告人许可，不公开其身份。

请只在你拥有或明确获准测试的环境中验证漏洞，避免读取他人数据、破坏服务、进行社交工程或造成不必要的可用性影响。
