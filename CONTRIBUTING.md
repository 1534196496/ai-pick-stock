# 贡献指南

感谢你愿意改进 AI Pick Stock。本项目面向个人投资者，提供股票与基金的持仓、自选和账户管理能力；它不是投顾服务，也不承诺行情实时性或投资收益。

## 贡献范围

欢迎以下贡献：

- 修复可复现的缺陷；
- 改善持仓、自选、账户和行情数据的正确性与可用性；
- 补充自动化测试、文档、无障碍和性能改进；
- 在符合上游服务条款和数据许可证的前提下改进数据源适配。

请不要提交以下内容：

- 用户数据、生产配置、Cookie、Token、密钥或真实账户截图；
- 未经授权复制的数据、接口或第三方受限内容；
- 宣称保证收益、替用户做投资决策或诱导交易的功能；
- 未经讨论的大范围重写、微服务拆分或不兼容接口变更。

## 开始之前

1. 搜索现有 Issue，确认问题尚未被记录或解决。
2. 缺陷请提供最小复现、期望结果、实际结果、运行环境和数据时间。
3. 新功能、数据库结构调整、依赖引入和 Breaking Change 请先创建 Issue，说明用户价值、兼容策略和迁移方案，等待维护者确认后再实现。
4. 安全问题不要公开提交，按 [安全政策](SECURITY.md) 报告。

## 开发约定

- 从约定的开发分支创建短期 feature/fix 分支，不直接修改受保护分支。
- 一次变更只解决一个关注点，避免混入无关格式化或重构。
- 新业务逻辑遵循 RED → GREEN → REFACTOR，并补充相应测试。
- Python 类和方法、TypeScript 导出组件和业务 Hook 应有清晰的中文职责说明；注释重点解释业务原因和非显而易见的取舍。
- 金额、成本、份额和收益计算使用十进制定点类型，不使用二进制浮点数作为权威结果。
- 已发布接口优先通过新增可选字段演进；不得静默删除字段、改变类型或改变既有语义。
- 代码、文档和样例不得包含秘密或个人敏感信息。

项目命令和目录仍在建设时，以仓库根目录的 `README.md`、`Makefile` 及 CI 配置为准；不要自行假设未声明的命令。

## 提交 Pull Request

Pull Request 应包含：

- 问题背景、变更范围和明确的不变范围；
- 关联 Issue；
- 自动化测试命令及结果；
- 界面变化的截图或录屏；
- 数据库、配置、数据源、安全和兼容性影响；
- Breaking Change 的迁移步骤与回滚方案；
- 不包含秘密和个人数据的确认。

提交信息采用 Conventional Commits，说明使用中文，例如：

```text
feat: 增加基金持仓快速录入
fix: 修复账户切换后汇总未刷新的问题
docs: 补充本地开发说明
```

维护者会重点检查需求一致性、数据隔离、财务计算、测试证据、向后兼容性和可回滚性。评审意见解决且自动化门禁通过，并不代表一定立即发布。

## 许可证

提交贡献即表示你有权提交相关内容，并同意贡献内容按仓库的 [Apache License 2.0](LICENSE) 许可发布。
